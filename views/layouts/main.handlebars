<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>expense tracker</title>
    <link rel="stylesheet" href="/stylesheets/bootstrap.css" />
    <link rel="stylesheet" href="/stylesheets/index.css" />

  </head>
  <body>
    <header>
      <nav class="navbar navbar-light bg-light">
        <div class="container-fluid px-5">
          <a class="navbar-brand text-dark fs-2" href="/">
            {{#if
              isAuthenticated
            }}{{user.name}}的記帳本{{else}}家庭記帳本{{/if}}</a>
          <div class="text-end">
            {{#if isAuthenticated}}
              <a href="/users/logout" class="btn btn-primary">登出</a>
            {{else}}
              <a href="/users/login" class="btn btn-primary">登入</a>
            {{/if}}
          </div>
        </div>
      </nav>
    </header>

    {{{body}}}
    <script
      src="https://kit.fontawesome.com/9db7c4d31f.js"
      crossorigin="anonymous"
    ></script>
    <script src="/javascripts/popper.js"></script>
    <script src="/javascripts/bootstrap.js"></script>
  </body>
</html>